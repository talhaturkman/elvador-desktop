{
  "name": "elvador-desktop-legacy",
  "version": "0.25.19-legacy.1",
  "private": true,
  "description": "Elvador legacy Windows 7 32-bit desktop shell",
  "author": "Elvador",
  "main": "src/main.js",
  "scripts": {
    "dev": "electron .",
    "pack": "electron-builder --dir",
    "build:win": "electron-builder --win nsis portable",
    "build:legacy": "electron-builder --win --ia32 --dir --publish never && node scripts/post-build-icon.js && electron-builder --win nsis portable --ia32 --prepackaged release/win-ia32-unpacked --publish never",
    "build:release": "electron-builder --win --dir --publish never && node scripts/write-app-update-yml.js && node scripts/post-build-icon.js && npx electron-builder --win nsis --prepackaged release/win-unpacked --publish never"
  },
  "dependencies": {
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "electron": "22.3.27",
    "electron-builder": "26.8.1",
    "rcedit": "^5.0.2",
    "sharp": "^0.34.5"
  },
  "build": {
    "appId": "com.elvador.desktop.legacy",
    "productName": "Elvador Legacy",
    "directories": {
      "output": "release"
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "assets/icon.ico",
        "to": "icon.ico"
      },
      {
        "from": "assets/icon-512.png",
        "to": "icon-512.png"
      }
    ],
    "publish": {
      "provider": "github",
      "owner": "talhaturkman",
      "repo": "elvador-desktop"
    },
    "win": {
      "artifactName": "Elvador-Legacy-${version}-${arch}.${ext}",
      "icon": "assets/icon.ico",
      "signAndEditExecutable": false,
      "target": [
        "nsis",
        "portable"
      ]
    },
    "nsis": {
      "artifactName": "Elvador-Legacy-Setup-${version}-${arch}.${ext}",
      "installerIcon": "assets/icon.ico",
      "uninstallerIcon": "assets/icon.ico",
      "oneClick": true,
      "perMachine": false,
      "createDesktopShortcut": "always",
      "createStartMenuShortcut": true,
      "shortcutName": "Elvador Legacy",
      "runAfterFinish": true
    }
  }
}
